 const superHero: string[] = []
 const heroPower:number[] = []//u can use either string or numbers
 const heroPowers: Array<number> = []// here is another way of declaring an array

 type User = {
    name: string
    isActive: boolean
 }

 const allUsers: User[] = []

 //complex ways

 const MLModels: number[][] = [
    [122, 122, 122],
    []
 ]// array of arrays 

 allUsers.push({name: "", isActive: true})
 export{}